#include <stdio.h>

/**
 * main - Entry point
 * main - Prints all single digit number
 * Return: Always 0
 */

int main(void)
{
	int  n;

	for (n = '0'; n <= '9'; n++)
	putchar(n + '0' );
	putchar('\n');

	for (n = 'a'; n <= 'f'; n++) 
	return (0);
}
