#include <stdio.h>

/**
 * main - Entry point
 * main - Prints all single digit number
 * Return: Always 0
 */

int main(void)
{
	char n;

	for (n = '0'; n <= '9'; n++)
	putchar(n);

	putchar('\n');
	return (0);
}
